---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import config from '../content/config/config.json';
import client from '../../tina/__generated__/client';
import HomePage from "../../tina/pages/HomePage.tsx";
const data = await client.queries.page({relativePath: "main.mdx"});

---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={data.data.page.seo?.title} description={data.data.page.seo?.description} />
	</head>
	<body>
		<Header />
		<HomePage {...data} client:tina />
		<Footer />
	</body>
</html>
